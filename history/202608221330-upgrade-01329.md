# Calcit 0.13.29 upgrade

- Updated the dependency snapshot and `@calcit/procs` lock entry to 0.13.29.
- Removed legacy `compact.cirru`; `calcit.cirru` is the only tracked snapshot and is validated with `calcit edit format`.
- Migrated test and npm-release workflows from `setup-cr`/`cr` to `setup-calcit`/`calcit`, with Node 24 and Yarn 4.12.
- Verified dependency resolution, immutable Yarn install, check-only validation, and JavaScript code generation.
